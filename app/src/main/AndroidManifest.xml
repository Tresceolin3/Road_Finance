<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roadfinance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.activity.cadastrar3Activity"></activity>
        <activity android:name=".activity.activity.cadastrar2Activity"/>
        <activity android:name=".activity.activity.cadastrarActivity" />
        <activity android:name=".activity.activity.loginActivity" />
        <activity
            android:name=".activity.activity.MainActivity"
            android:theme="@style/Theme.Intro"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>